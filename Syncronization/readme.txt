1.compile with g++ syncronize.cpp -o syncronize
2.Run with ./syncronize
3.Programme is checked with all 4 experiments except boundary conditions and results are given in report
4.Try to keep obvious cases like threads less than size of matrix,row increment divisor of size of matrix etc
